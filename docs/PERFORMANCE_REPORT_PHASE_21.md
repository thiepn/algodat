# Performance Report Phase 21

Die Simulatorroute bleibt per Router lazy geladen. Die strukturierten Renderer verwenden die bereits vorhandenen lokalen Trainer-Engines; es gibt keine neuen externen Dienste, PDFs oder Bildmaterialien. Der verbindliche Bundlewert wird vor dem Release über `npm.cmd run analyze:bundle` erhoben.
