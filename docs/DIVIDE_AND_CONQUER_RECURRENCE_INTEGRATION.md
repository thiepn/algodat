# Divide-and-Conquer Rekurrenzintegration

Die Rekurrenz wird strukturiert erfasst:

- Gleichung: `T(n)=2T(n/2)+O(1)`,
- Combine-Kosten: `O(1)`,
- Laufzeit: `O(n)`.

Die bestehende Rekurrenzarchitektur bleibt unverändert. Phase 13 führt keinen zweiten Master-Theorem-Parser ein.
