import { PlannedState } from '../../ui/feedback/PlannedState';

export function SimulatorPlannedPage() {
  return (
    <PlannedState
      title="Klausursimulator"
      description="Der vollständige Klausursimulator bleibt einer späteren Phase vorbehalten."
    />
  );
}
